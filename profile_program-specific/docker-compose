safe_alias d docker-compose

dbash() {
  docker-compose exec $1 bash
}

safe_alias dbundle 'docker-compose exec rails bundle'
safe_alias drails 'docker-compose exec rails bundle exec rails'
safe_alias drake 'docker-compose exec rails bundle exec rake'
safe_alias drspec 'docker-compose exec rails bundle exec rspec'
