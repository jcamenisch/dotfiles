. ~/.bashrc

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/jcamenisch/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
[[ -f ~/bin/tmuxinator.bash ]] && source ~/bin/tmuxinator.bash
