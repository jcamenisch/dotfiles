alias ls="ls -G"
alias lsl="ls -l"
alias lsa="ls -a"
alias lsla="ls -la"

alias profile=". ~/.profile"

alias vip="vi ~/.profile"

alias ssh2="ssh 2.portcat.net"
alias ssh2r="ssh -R 2222:localhost:22 2.portcat.net"
sshmt () { ssh serveradmin@$1@$1 ; }

mdcd () { mkdir $1 ; cd $1 ; }
