alias ls="ls -G"
alias lsl="ls -l"
alias lsa="ls -a"
alias lsla="ls -la"

alias profile=". ~/.profile"

alias vip="vi ~/.profile"

alias sshp="ssh pc.camenischcreative.com"
sshmt () { ssh serveradmin@$1@$1 ; }

mdcd () { mkdir $1 ; cd $1 ; }
